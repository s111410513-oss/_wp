generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Score {
  id        Int      @id @default(autoincrement())
  playerName String
  attempts  Int
  createdAt DateTime @default(now())
}
